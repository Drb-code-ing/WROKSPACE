# Day 33 Blog v037 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish v037 as a standalone Day 33 article covering MCP Resource context injection, local/remote multi-server orchestration, lifecycle handling, and an honestly unresolved remote-tool debugging session.

**Architecture:** Treat one blog version as a five-file atomic unit: three files under `blogs/versions/v037/`, plus `blogs/CURRENT` and one appended manifest entry. Build the article from commits `112a486` and `6097c6a`, use v034-v036 only as a duplication boundary, and validate metadata, source coverage, security, Markdown structure, and Git scope before committing.

**Tech Stack:** Markdown, JSON, Git, PowerShell 7, ripgrep (`rg`)

---

## File Map

- Create `blogs/versions/v037/blog.md`: the publishable Day 33 article.
- Create `blogs/versions/v037/outline.md`: the article's scope and section contract.
- Create `blogs/versions/v037/coverage.json`: Day 33 source provenance and highlights.
- Modify `blogs/CURRENT`: select v037 as the current article.
- Modify `blogs/manifest.json`: append exactly one v037 version entry.
- Read only `blogs/versions/v034/`, `blogs/versions/v035/`, and `blogs/versions/v036/`: enforce the no-duplication boundary.
- Do not touch `.trae/` or `ai/agent/agent_in_action/hello-langchain/react-todo-app/`.

### Task 1: Establish the Baseline and the Failing v037 Contract

**Files:**

- Read: `docs/superpowers/specs/2026-07-10-day33-blog-design.md`
- Read: `blogs/CURRENT`
- Read: `blogs/manifest.json`
- Read: `blogs/versions/v036/coverage.json`

- [ ] **Step 1: Confirm the branch, upstream relationship, and protected user files**

Run:

```powershell
git status --short --branch
git rev-parse --show-toplevel
$gitDir = (Resolve-Path (git rev-parse --git-dir)).Path
$gitCommonDir = (Resolve-Path (git rev-parse --git-common-dir)).Path
"git-dir: $gitDir"
"git-common-dir: $gitCommonDir"
git rev-list --left-right --count origin/master...HEAD
```

Expected before remote refresh:

- Branch is `master` with upstream `origin/master`.
- Git directory and common directory both resolve to `E:\WROKSPACE\.git`.
- The only untracked paths are `.trae/` and `ai/agent/agent_in_action/hello-langchain/react-todo-app/`.
- Local HEAD is ahead by the already approved documentation commits and has no tracked working-tree edits.

- [ ] **Step 2: Refresh `origin/master` and stop if the local branch is behind**

Run:

```powershell
git fetch origin master
$counts = (git rev-list --left-right --count origin/master...HEAD) -split '\s+'
if ([int]$counts[0] -ne 0) {
  throw "origin/master has commits not present locally; reconcile before editing v037"
}
```

Expected: exit code 0. A nonzero left count is a hard stop; do not rebase, merge, or force-push automatically.

- [ ] **Step 3: Run the structural contract before implementation**

Run:

```powershell
$version = 'v037'
$dir = "blogs/versions/$version"
$manifest = Get-Content -Raw -Encoding UTF8 'blogs/manifest.json' | ConvertFrom-Json
$failures = @()
if (-not (Test-Path "$dir/blog.md")) { $failures += 'blog.md missing' }
if (-not (Test-Path "$dir/outline.md")) { $failures += 'outline.md missing' }
if (-not (Test-Path "$dir/coverage.json")) { $failures += 'coverage.json missing' }
if ((Get-Content -Raw -Encoding UTF8 'blogs/CURRENT').Trim() -ne $version) { $failures += 'CURRENT is not v037' }
if (@($manifest.versions | Where-Object id -eq $version).Count -ne 1) { $failures += 'manifest v037 count is not 1' }
if ($failures.Count -eq 0) { throw 'Pre-implementation contract unexpectedly passed' }
$failures
```

Expected: the command lists all five missing-contract symptoms. This is the red phase for the content artifact.

### Task 2: Create the v037 Outline

**Files:**

- Create: `blogs/versions/v037/outline.md`

- [ ] **Step 1: Create the version directory and outline with this exact structure**

Use `apply_patch` to create `blogs/versions/v037/outline.md` with the following content:

```markdown
# v037 大纲

## 标题
从工具调用到上下文编排：MCP Resource、多 Server 协作与远程工作流排障

## 主题
第三十三天学习——上篇补全 MCP Resource 的注册、读取与上下文注入；下篇把本地、远程和 npx 启动的 MCP Server 接入同一个 Agent，并如实记录一次尚未结束的远程工具排障。

## 核心线索
v036 解决“Agent 如何跨进程调用工具”，v037 继续回答两个问题：Server 如何向模型提供知识，多种 Server 如何组成可观察、可释放、可排障的真实工作流。

## 章节结构

1. **引言** — 从“工具已经跨进程”推进到“工具与资源如何共同组成上下文”
2. **一、MCP Resource** — registerResource、资源 URI、MIME 类型、Tool 与 Resource 的职责边界
3. **二、从 Resource 到 SystemMessage** — listResources、readResource、Object.entries、直接注入与 RAG 的边界
4. **三、从单 Server 到多 Server** — 本地自建、高德 HTTP、Chrome DevTools、FileSystem 四类能力的统一编排
5. **四、工程闭环** — 工具结果归一化、异常回传、ToolMessage 关联与 mcpClient.close 生命周期
6. **五、真实排障** — maps_geo 调用挂起的现象、假设和后续验证顺序，明确状态仍为 OPEN
7. **六、AI 工程化认知升级** — 从调用工具到编排上下文，从展示成功到保留失败证据
8. **结语** — Resource、多 Server 与可观测排障构成第三十三天的新增能力

## 核心代码

- my-mcp-server.mjs：registerResource 注册 docs://guide 静态资源
- langchain-mcp-test.mjs：listResources/readResource + Object.entries + SystemMessage 注入
- remote-mcp/src/mcp-test.mjs：四类 MCP Server 配置、工具结果适配、异常回传与连接关闭
- debug-map-tool-hang.md：未解决挂起问题的症状和假设记录

## 面试要点

- MCP Tool 与 Resource 的职责差异
- Resource 直接注入上下文与 RAG 检索的适用边界
- 本地 stdio、远程 HTTP 与 npx Server 在同一客户端中的生命周期管理
- 多工具工作流出现挂起时的隔离、超时、日志和逐层恢复策略

## 情感线
真正的工程能力不只体现在 Demo 跑通，也体现在失败发生时能留下证据、缩小变量并诚实标记未知。
```

- [ ] **Step 2: Check the outline shape**

Run:

```powershell
rg -n '^#{1,2} ' 'blogs/versions/v037/outline.md'
```

Expected: one H1 followed by the seven named H2 sections in the exact order above.

### Task 3: Write the v037 Article

**Files:**

- Create: `blogs/versions/v037/blog.md`
- Reference: `ai/agent/agent_in_action/mcp-demo/1.js`
- Reference: `ai/agent/agent_in_action/mcp-demo/readme.md`
- Reference: `ai/agent/agent_in_action/mcp-demo/my-mcp-server.mjs`
- Reference: `ai/agent/agent_in_action/mcp-demo/langchain-mcp-test.mjs`
- Reference: `ai/agent/agent_in_action/remote-mcp/readme.md`
- Reference: `ai/agent/agent_in_action/remote-mcp/src/mcp-test.mjs`
- Reference: `ai/agent/agent_in_action/remote-mcp/debug-map-tool-hang.md`

- [ ] **Step 1: Write the introduction and Resource section**

Create `blog.md` with this exact H1 and section order:

```markdown
# 从工具调用到上下文编排：MCP Resource、多 Server 协作与远程工作流排障

## 引言
## 一、MCP Resource：让 Server 不只提供动作，也提供知识
## 二、从 Resource 到 SystemMessage：上下文注入的数据流
## 三、从单 Server 到多 Server：本地与远程能力编排
## 四、工程闭环：结果适配、异常可观察与资源释放
## 五、真实排障：为什么 maps_geo 调用后挂起
## 六、AI 工程化认知升级
## 结语
```

The introduction must make only these claims:

- v036 already established MCP tool calling across processes; do not re-teach MCP, stdio, HTTP, ReAct, Promise, or child processes.
- Day 33 adds Resource context, multi-server composition, lifecycle handling, and debugging evidence.
- The remote hotel workflow is an intended orchestration scenario, not a verified successful result.

In section one, explain Tool as an action and Resource as readable context. Include this redacted, runnable example:

```javascript
server.registerResource(
  '使用指南',
  'docs://guide',
  {
    description: 'MCP Server 使用指南',
    mimeType: 'text/plain',
  },
  async () => ({
    contents: [{
      uri: 'docs://guide',
      mimeType: 'text/plain',
      text: '功能：提供用户查询等工具。使用：通过 MCP Client 读取本指南。',
    }],
  }),
)
```

- [ ] **Step 2: Explain the Resource-to-context data flow without repeating prior Message tutorials**

Section two must show this pipeline:

```text
registerResource
      ↓
listResources → Object.entries 遍历 Server
      ↓
readResource(serverName, resource.uri)
      ↓
拼接 resourceContent
      ↓
SystemMessage(resourceContent) → 模型上下文
```

Use this source-faithful code excerpt:

```javascript
const resourcesByServer = await mcpClient.listResources()
let resourceContent = ''

for (const [serverName, resources] of Object.entries(resourcesByServer)) {
  for (const resource of resources) {
    const content = await mcpClient.readResource(serverName, resource.uri)
    resourceContent += content[0].text
  }
}

const messages = [
  new SystemMessage(resourceContent || '(无资源)'),
  new HumanMessage(query),
]
```

Tie the small `Object.entries()` exercise to this traversal. State the boundary precisely: short, known resources can be injected directly; long or numerous documents should be retrieved selectively with RAG to avoid consuming the context window.

- [ ] **Step 3: Explain multi-server orchestration with secret-safe configuration**

Section three must distinguish four configured capabilities without re-explaining transport definitions:

| Server | Launch/connection | Intended capability |
|---|---|---|
| Local guide server | `node` command | user lookup Tool + guide Resource |
| AMap server | remote HTTP URL from environment | geocoding and nearby-place lookup |
| Chrome DevTools | `npx` command | browser tabs and page inspection |
| FileSystem | `npx` command with an allowed root | local file operations |

Use this configuration; never copy the committed map key or an authenticated remote URL:

```javascript
const mcpClient = new MultiServerMCPClient({
  localGuide: {
    command: 'node',
    args: ['./mcp-demo/my-mcp-server.mjs'],
  },
  amapMaps: {
    transport: 'http',
    url: process.env.AMAP_MCP_URL,
  },
  chromeDevtools: {
    command: 'npx',
    args: ['-y', 'chrome-devtools-mcp@latest'],
  },
  filesystem: {
    command: 'npx',
    args: ['-y', '@modelcontextprotocol/server-filesystem', process.cwd()],
  },
})
```

Describe the hotel scenario as an orchestration target: map lookup produces candidates, browser tools display pages, and file tools may persist results. Explicitly state that the submitted run stopped during the map step.

- [ ] **Step 4: Explain result adaptation, failure visibility, and lifecycle cleanup**

Section four must distinguish the current learning code from a hardened variant. Show this hardened result adapter as an engineering improvement:

```javascript
let contentStr

try {
  const toolResult = await foundTool.invoke(toolCall.args)
  contentStr = typeof toolResult === 'string'
    ? toolResult
    : toolResult?.text ?? JSON.stringify(toolResult)
} catch (error) {
  contentStr = `工具 ${toolCall.name} 调用失败：${error.message}`
}

messages.push(new ToolMessage({
  content: contentStr,
  tool_call_id: toolCall.id,
}))
```

Explain that the source calls `mcpClient.close()` after the run and that production code should guarantee cleanup with `finally`:

```javascript
try {
  await runAgentWithTools(query)
} finally {
  await mcpClient.close()
}
```

Keep `ToolMessage` and `tool_call_id` to one paragraph because v034 already covered their mechanics.

- [ ] **Step 5: Write the unresolved-debugging section and conclusion**

Section five must record the exact known state:

- Symptom: output reaches the first iteration and `maps_geo`, then no later result appears.
- Status: OPEN.
- Unverified hypotheses: remote HTTP call does not return; arguments do not match the server schema; result handling does not complete; another initialized Server interferes.
- Next verification order: call `maps_geo` directly; log tool schema and arguments; add an explicit timeout; run with only AMap configured; restore the other Servers one at a time.

Do not use wording that says or implies the hotel workflow succeeded. Section six and the conclusion must summarize three Day 33 gains only: Resource as context, multi-server orchestration, and evidence-driven debugging/lifecycle discipline.

End the file with:

```markdown
---

*本篇内容基于第三十三天学习笔记整理，重点记录 MCP Resource、多 Server 编排与一次尚未结束的远程工具排障。*
```

### Task 4: Add Provenance and Version Indexes

**Files:**

- Create: `blogs/versions/v037/coverage.json`
- Modify: `blogs/CURRENT`
- Modify: `blogs/manifest.json`

- [ ] **Step 1: Create `coverage.json` with exact metadata and sources**

Use this JSON:

```json
{
  "version": "v037",
  "date": "2026-07-10",
  "day": 33,
  "title": "从工具调用到上下文编排：MCP Resource、多 Server 协作与远程工作流排障",
  "sources": [
    {
      "path": "ai/agent/agent_in_action/mcp-demo/1.js",
      "topic": "Object.entries 对象遍历与资源映射处理",
      "hash": "auto"
    },
    {
      "path": "ai/agent/agent_in_action/mcp-demo/readme.md",
      "topic": "MCP Resource、Context 注入与 RAG 边界笔记",
      "hash": "auto"
    },
    {
      "path": "ai/agent/agent_in_action/mcp-demo/my-mcp-server.mjs",
      "topic": "registerResource、docs URI 与静态使用指南",
      "hash": "auto"
    },
    {
      "path": "ai/agent/agent_in_action/mcp-demo/langchain-mcp-test.mjs",
      "topic": "资源枚举读取、SystemMessage 注入、Agent 循环与客户端关闭",
      "hash": "auto"
    },
    {
      "path": "ai/agent/agent_in_action/remote-mcp/readme.md",
      "topic": "地图、浏览器与文件系统 MCP 应用场景",
      "hash": "auto"
    },
    {
      "path": "ai/agent/agent_in_action/remote-mcp/src/mcp-test.mjs",
      "topic": "本地与远程多 MCP Server 编排、结果适配与异常处理",
      "hash": "auto"
    },
    {
      "path": "ai/agent/agent_in_action/remote-mcp/debug-map-tool-hang.md",
      "topic": "maps_geo 挂起现象、未验证假设与开放排障状态",
      "hash": "auto"
    }
  ],
  "highlights": [
    "MCP Resource 从服务端注册到 SystemMessage 注入的完整数据流",
    "Object.entries 在多 Server 资源遍历中的实际应用",
    "短资源直接注入与长文档 RAG 检索的边界",
    "本地、远程 HTTP 与 npx MCP Server 的统一编排",
    "工具返回值归一化、异常回传与 mcpClient.close 生命周期",
    "maps_geo 挂起问题的诚实记录与隔离排查顺序"
  ]
}
```

- [ ] **Step 2: Set `CURRENT` to v037**

Replace the complete content of `blogs/CURRENT` with exactly:

```text
v037
```

- [ ] **Step 3: Append one manifest version entry**

Append this object after the v036 object in `blogs/manifest.json` without reformatting unrelated entries:

```json
{
  "id": "v037",
  "date": "2026-07-10",
  "title": "从工具调用到上下文编排：MCP Resource、多 Server 协作与远程工作流排障",
  "covered_sources": {
    "ai/agent/agent_in_action/mcp-demo/1.js": "auto",
    "ai/agent/agent_in_action/mcp-demo/readme.md": "auto",
    "ai/agent/agent_in_action/mcp-demo/my-mcp-server.mjs": "auto",
    "ai/agent/agent_in_action/mcp-demo/langchain-mcp-test.mjs": "auto",
    "ai/agent/agent_in_action/remote-mcp/readme.md": "auto",
    "ai/agent/agent_in_action/remote-mcp/src/mcp-test.mjs": "auto",
    "ai/agent/agent_in_action/remote-mcp/debug-map-tool-hang.md": "auto"
  }
}
```

Do not add missing historical versions and do not modify `source_files`.

### Task 5: Verify Content, Metadata, Security, and Scope

**Files:**

- Test: `blogs/versions/v037/blog.md`
- Test: `blogs/versions/v037/outline.md`
- Test: `blogs/versions/v037/coverage.json`
- Test: `blogs/CURRENT`
- Test: `blogs/manifest.json`

- [ ] **Step 1: Run the complete structural and provenance verifier**

Run:

```powershell
$version = 'v037'
$title = '从工具调用到上下文编排：MCP Resource、多 Server 协作与远程工作流排障'
$dir = "blogs/versions/$version"
$manifest = Get-Content -Raw -Encoding UTF8 'blogs/manifest.json' | ConvertFrom-Json
$coverage = Get-Content -Raw -Encoding UTF8 "$dir/coverage.json" | ConvertFrom-Json
$entry = @($manifest.versions | Where-Object id -eq $version)
$h1 = @(rg -n '^# ' "$dir/blog.md")

if ((Get-Content -Raw -Encoding UTF8 'blogs/CURRENT').Trim() -ne $version) { throw 'CURRENT mismatch' }
if ($entry.Count -ne 1) { throw 'manifest must contain exactly one v037 entry' }
if ($coverage.version -ne $version -or $coverage.day -ne 33 -or $coverage.date -ne '2026-07-10') { throw 'coverage metadata mismatch' }
if ($entry[0].title -ne $title -or $coverage.title -ne $title) { throw 'title mismatch' }
if ($h1.Count -ne 1 -or $h1[0] -notmatch [regex]::Escape($title)) { throw 'blog H1 mismatch' }
if ((Get-Content -Encoding UTF8 "$dir/blog.md" -TotalCount 1) -eq '---') { throw 'front matter is not allowed' }

$coveragePaths = @($coverage.sources.path | Sort-Object)
$manifestPaths = @($entry[0].covered_sources.PSObject.Properties.Name | Sort-Object)
if (Compare-Object $coveragePaths $manifestPaths) { throw 'coverage and manifest source paths differ' }
foreach ($path in $coveragePaths) {
  if (-not (Test-Path -LiteralPath $path)) { throw "missing source path: $path" }
}

'v037 structural verification passed'
```

Expected: `v037 structural verification passed` and exit code 0.

- [ ] **Step 2: Run security and truthfulness checks**

Run:

```powershell
$blog = 'blogs/versions/v037/blog.md'
$secretHits = rg -n 'mcp\.amap\.com/mcp\?key=|https?://[^\s]+@' $blog
if ($LASTEXITCODE -eq 0) { throw "credential-bearing URL found:`n$secretHits" }
$global:LASTEXITCODE = 0

$falseSuccess = rg -n '成功跑通|完整跑通|已经跑通|最终跑通' $blog
if ($LASTEXITCODE -eq 0) { throw "false success claim found:`n$falseSuccess" }
$global:LASTEXITCODE = 0

rg -n 'maps_geo|Status: OPEN|状态仍为 OPEN|尚未结束' $blog
```

Expected: no credential-bearing URL, no success claim, and matches proving the unresolved state is explicit.

- [ ] **Step 3: Review the no-duplication boundary against v034-v036**

Run:

```powershell
rg -n '^## |^### ' 'blogs/versions/v034/blog.md' 'blogs/versions/v035/blog.md' 'blogs/versions/v036/blog.md' 'blogs/versions/v037/blog.md'
```

Manual assertions:

- v037 has no standalone sections teaching Promise, ReAct, child processes, path/fs, MCP definitions, or stdio-vs-HTTP basics.
- `ToolMessage` mechanics occupy no more than one paragraph.
- The only v036 recap is the transition in the introduction.

- [ ] **Step 4: Check Markdown fences, JSON parsing, and whitespace**

Run:

```powershell
$blog = Get-Content -Raw -Encoding UTF8 'blogs/versions/v037/blog.md'
$fences = ([regex]::Matches($blog, '(?m)^```')).Count
if ($fences % 2 -ne 0) { throw 'unbalanced fenced code blocks' }
Get-Content -Raw -Encoding UTF8 'blogs/manifest.json' | ConvertFrom-Json | Out-Null
Get-Content -Raw -Encoding UTF8 'blogs/versions/v037/coverage.json' | ConvertFrom-Json | Out-Null
git diff --check -- blogs
```

Expected: exit code 0 with no output from `git diff --check`.

### Task 6: Create the Atomic Blog Commit

**Files:**

- Stage only the five v037 blog files.

- [ ] **Step 1: Inspect the unstaged change set**

Run:

```powershell
git status --short
git diff --stat -- blogs
git diff -- blogs/CURRENT blogs/manifest.json
```

Expected: status identifies only the five planned blog paths plus the two pre-existing untracked directories. Before staging, `git diff --stat` reports the two modified index files while the three new version files appear through `git status`.

- [ ] **Step 2: Stage explicit paths and verify the index**

Run:

```powershell
git add -- blogs/CURRENT blogs/manifest.json blogs/versions/v037/blog.md blogs/versions/v037/outline.md blogs/versions/v037/coverage.json
git diff --cached --check -- blogs
$staged = @(git diff --cached --name-only)
$expected = @(
  'blogs/CURRENT',
  'blogs/manifest.json',
  'blogs/versions/v037/blog.md',
  'blogs/versions/v037/coverage.json',
  'blogs/versions/v037/outline.md'
) | Sort-Object
if (Compare-Object ($staged | Sort-Object) $expected) { throw 'staged file set is not the five-file v037 unit' }
git diff --cached --stat
```

Expected: exactly five staged paths and no whitespace errors.

- [ ] **Step 3: Commit with the repository's versioned-blog convention**

Run:

```powershell
git commit -m "v037: 从工具调用到上下文编排：MCP Resource、多 Server 协作与远程工作流排障"
```

Expected: one new commit containing exactly the five v037 blog files.

- [ ] **Step 4: Re-run fresh post-commit verification**

Run the complete verifier from Task 5 Step 1 again, then run:

```powershell
git show --check --stat --oneline HEAD
git diff-tree --no-commit-id --name-only -r HEAD
git status --short --branch
```

Expected: verification passes; HEAD contains exactly the five blog paths; only the two protected untracked directories appear in status.

### Task 7: Push Only to origin/master and Verify the Remote State

**Files:**

- No file changes.

- [ ] **Step 1: Refresh the target remote and reject divergence**

Run:

```powershell
git fetch origin master
$counts = (git rev-list --left-right --count origin/master...HEAD) -split '\s+'
if ([int]$counts[0] -ne 0) { throw 'origin/master advanced; stop before push' }
if ([int]$counts[1] -lt 1) { throw 'no local commit available to push' }
"Ahead by $($counts[1]); safe to push"
```

Expected: remote-behind count is 0 and local-ahead count includes the approved design/plan documentation and v037 blog commits.

- [ ] **Step 2: Push the named branch to the named remote**

Run:

```powershell
git push origin master
```

Expected: push succeeds. Do not push to the separate `gitee` remote and never print its credential-bearing URL.

- [ ] **Step 3: Verify upstream parity and final workspace scope**

Run:

```powershell
git rev-list --left-right --count origin/master...HEAD
git status --short --branch
git log -3 --oneline
```

Expected:

- Ahead/behind is `0 0`.
- `master` tracks `origin/master` with no tracked or staged changes.
- The two pre-existing untracked directories remain unchanged.
- Recent history contains the design documentation, this implementation plan, and the v037 blog commit.
